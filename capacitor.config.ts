import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lainy.vending_machine_map',
  appName: 'vending_machine_map',
  webDir: 'www'
};

export default config;
